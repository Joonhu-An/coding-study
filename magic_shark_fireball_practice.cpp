#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int N, M, K;

struct Fire{
    int r, c, m, s, d;
};

vector<Fire> fvect;

/*  freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    */